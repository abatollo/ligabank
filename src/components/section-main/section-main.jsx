import SectionConverter from '../section-converter/section-converter';
import SectionHistory from '../section-history/section-history';

const SectionMain = () => {
  return (
    <main>
      <SectionConverter />
      <SectionHistory />
    </main>
  );
}

export default SectionMain;
